Placeholder images generated as SVG so the repo stays lightweight and needs
no external network requests. Replace groom.svg / bride.svg / gallery-*.svg
with real photos (jpg/png/webp) and update the matching paths in
../data.json — no HTML edits needed.
