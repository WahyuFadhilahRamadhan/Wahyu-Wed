Put the background music file for this couple here as `music.mp3`
(referenced directly in `../script.js`).

No audio file is bundled by default — the play/pause button will show a
toast and stay silent until you add one. Keep the file small (a
compressed MP3 under ~3–4 MB) so the page stays fast to load.
